#include <bits/stdc++.h>
using namespace std;

int main() {
    int c, n; cin >> c;
    for (int i = 0; i < c; i++) {
        cin >> n;
        int sum = 0;
        for (int i = 0; i < n; i++) {
            int tmp; cin >> tmp;
            sum += tmp;
        }
        
    }
}
