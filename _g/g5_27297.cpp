#include <iostream>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int dimention, number;
    cin >> dimention >> number;
    for (int i = 0; i < number; i++) {
        
        ;
    }
}