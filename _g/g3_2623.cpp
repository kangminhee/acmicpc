#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, m; cin >> n >> m;

    vector<int> v(1010);
    vector<int> r(1010);

    int k, tmp; cin >> k;
    for (int i = 0; i < k; i++) {
        cin >> tmp;
        v.push_back(tmp);
    }
}